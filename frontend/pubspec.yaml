name: streaming_bola_app
description: Aplikasi Streaming Bola
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP Client
  http: ^1.1.0
  
  # Video Player
  video_player: ^2.7.0
  chewie: ^1.5.0
  video_player_web_hls: ^1.2.1
  better_player: ^0.0.83
  
  # State Management
  provider: ^6.0.5
  
  # UI Components
  flutter_spinkit: ^5.2.0
  cached_network_image: ^3.3.0
  intl: ^0.18.1
  
  # Navigation
  go_router: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/videos/